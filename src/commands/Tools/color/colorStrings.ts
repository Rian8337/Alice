/**
 * Strings for the `color` command.
 */
export enum colorStrings {
    invalidHexCode = "I'm sorry, that doesn't look like a valid hex code color!",
    showingHexColor = "Showing color with hex code `%s`:",
}
