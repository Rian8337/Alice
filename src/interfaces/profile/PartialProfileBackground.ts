/**
 * Partial profile background object used for
 * storing backgrounds in `PlayerInfo` instances.
 */
export interface PartialProfileBackground {
    /**
     * The ID of the badge.
     */
    id: string;

    /**
     * The name of the badge.
     */
    name: string;
};